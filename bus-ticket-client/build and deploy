#!groovy?
pipeline {
    agent any
    stages{
        stage('deploy'){
            steps{
                script{
                    sh '''
                        cd /app/compose && docker compose up -d
                    '''    
                }
            }
        }
        
    }
    post{
        always{
            cleanWs()
        }
        
    }
}

